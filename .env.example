# Tana API Configuration
# Copy this file to .env and fill in your actual values
# WARNING: Never commit your .env file with real credentials!

# Required for API sync mode
# Get this from Tana settings
TANA_API_TOKEN=your-token-here

# Optional: Supertag ID for API sync
# How to get your supertag node ID:
#   Method 1: Open supertag → Configuration panel → "Show API Schema" → Copy supertag id
#   Method 2: Right-click supertag → "Copy link" → Extract ID after "nodeid="
# Example: For a supertag like "task (Tanarian Brain)", get its node ID
SUPERTAG_ID=your-supertag-node-id

# Optional: Supertag name for clipboard sync mode
# This is the human-readable name, not the ID
# Example: "task" or "task (Tanarian Brain)"
SUPERTAG_NAME=task

# Optional: Target node ID for "Today" tasks
# Defaults to INBOX if not set
TANA_TODAY_NODE_ID=INBOX
